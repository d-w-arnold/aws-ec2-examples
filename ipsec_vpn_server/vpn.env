# Define your own values for these variables
# - DO NOT put "" or '' around values, or add space around =
# - DO NOT use these special characters within values: \ " '
#VPN_IPSEC_PSK=secrets_manager_secret_vpn_ipsec_psk
VPN_USER=vpn_sih
#VPN_PASSWORD=secrets_manager_secret_vpn_password

# (Optional) Define additional VPN users
# - Uncomment and replace with your own values
# - Usernames must be separated by spaces
# - Passwords for each additional user are generated by CDK and stored in AWS Secrets Manager
VPN_ADDL_USERS="davida sih-ios sih-android"

# (Optional) Specify a name for the first IKEv2 client
# - Use one word only, no special characters except '-' and '_'
# - The default is 'vpnclient' if not specified
VPN_CLIENT_NAME=vpnclient_vpc-01-sih

# (Optional) Use alternative DNS servers
# - Below IP address are Cloudflare's DNS service
VPN_DNS_SRV1=1.1.1.1
VPN_DNS_SRV2=1.0.0.1

# Allow ssh access to the instance
EC2_SSH_ALLOWED=True
